# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'rake', '~> 10.0'
gem 'rspec', '~> 3.0'
gem 'rubocop', '>= 1.53', require: false

# Specify your gem's dependencies in repo-drift-detector.gemspec
gemspec
