RSpec.describe Repo::Drift::Detector do
  it "has a version number" do
    expect(Repo::Drift::Detector::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
